"""Stage 4 — infer (heavy lane): segment every case's tray through the SAME TS engine the browser runs
(frontend/src/cv/, via tsx) — the classical baseline classifier now, the CNN once trained — and bake the deterministic
per-case outputs to data/derived/case-results.json. Delegates to `cllab/science/bake_cases.mjs`."""
